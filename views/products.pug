html
    head
    body
        table
            each p in products
                tr
                    td=p.id
                    td=p.name
                    td=p.price
                    td=p.category_name
                    td
                    if userRole==="ADMIN"
                        a(href="/edit/"+p.id) Edit product
                        td
                            form(action="/delete/"+p.id method="POST")
                                //input(type="hidden" name="_method" value="DELETE")
                                input(type="submit" value="DELETE")
        if userRole==="ADMIN"
            a(href="/createProd") create product